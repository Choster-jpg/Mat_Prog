#pragma once

#include <iostream>
#include <iomanip>
#include "Combi.h"
#include "Boat.h"

#define N (sizeof(AA)/2)
#define M 3
#define conteinersCount 8

#define random(min, max) min + rand() % (max - min);
